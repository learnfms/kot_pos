name: kot_pos
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: any
  get: any
  geocoding: any
  shared_preferences: any
  web_socket_channel: ^3.0.2
  drift: ^2.13.0
  sqlite3_flutter_libs: ^0.5.0
  path_provider: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  # drift_dev: ^2.13.0
  build_runner: ^2.4.0
  drift_dev: ^2.13.0  # Only if using Drift/SQLite

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/images/background.png
